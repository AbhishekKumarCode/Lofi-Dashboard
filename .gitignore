# Android
*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# App build outputs
app/build/

# Node (for server folder)
node_modules/
npm-debug.log

# OS
.DS_Store
Thumbs.db

# Keys — never commit signing keys
*.jks
*.keystore

# Dashboard user settings (theme/wallpaper prefs — local only)
dashboard-server/settings.json

# Control panel build output and deps
control-panel/dist/
control-panel/node_modules/

# Never commit built binaries
*.exe
